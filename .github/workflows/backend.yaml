name: Backend
on:
  pull_request:
    branches: master
    paths-ignore: |
      ./frontend/**

jobs:
  Test:
    runs-on: ubuntu-latest
    steps:
      - name: Pull code
        uses: actions/checkout@v3
      - name: install dependencies
        run: npm i
      - name: Test
        run: echo "Testing"
